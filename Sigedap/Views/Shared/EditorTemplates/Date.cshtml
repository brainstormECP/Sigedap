    @inherits System.Web.Mvc.WebViewPage<DateTime?>
    @if (Model.HasValue)
    {
        @Html.TextBox("", (Model.Value),"{0:dd/MM/yyyy}", new { @class = "form-control datepicker" , @type = "date" })
    }
    else
    {
        @Html.TextBox("", DateTime.Now,"{0:dd/MM/yyyy}", new { @class = "form-control datepicker" , @type = "date" })
    }
