@model IEnumerable<Sigedap.Models.DictamenPorPartes>

@{
    ViewBag.Title = "Index";
}
<div class = "panel panel-default">
<div class = "panel-heading">
<p>
    @Html.ActionLink("Nuevo DictamenPorPartes", "Create",null, new {@class = "btn btn-success"})
</p>
</div>
<div class = "panel-body">
<table class="table dynamic">
<thead>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DictamenTecnico.Tecnico1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Defecto)
        </th>
        <th></th>
    </tr>
</thead>
<tbody>
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DictamenTecnico.Tecnico1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Defecto)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.Id }, new {@class = "btn btn-info"}) |            
            @Html.ActionLink("Eliminar", "Delete", new { id=item.Id }, new {@class = "btn btn-danger"})
        </td>
    </tr>
}
<tbody>
</table>
</div>
</div>

@section Scripts {
    <script src="~/Scripts/jquery.dataTables.js"></script>
    <script src="~/Scripts/mbs-qdtf.js"></script>
}

